<?php
/**
 * Template Name: Front Page
 */

get_header(); ?>

<div class="w-full">
    <!-- Hero Section -->
    <div class="hero" style='background-image: url("https://lh3.googleusercontent.com/aida-public/AB6AXuDiz6btUyMkFOcrDMO3R-TBVwk6BY4Ba4CtO1dPsWs0Qta686lNVGoJBPvZamNQykUcbjSjEtFs_qoUHXELAElYfwqmYUfTPYT5JM_uUkDm527Z2WjROjEKGNMzx99fxuVziJPpsszqg6DFa64lW4vhTachxSmNQ6jL8teY3LdW14p_aFrJW7apBqAd3--B_eBvYFihDrN5pFGD5cgWc_RLzoQwuek_4h29D9nmkjoW1n-zQTyWOIAKORgzcG8iUVrHY1p0L35THEA");'>
        <div class="hero-content">
            <h1>Your Private Journey Through Morocco Starts Here</h1>
            <p>Premium Private Drivers, Airport Transfers, and Custom Excursions in Marrakech.</p>
            
            <form role="search" method="get" class="hero-search" action="<?php echo esc_url( home_url( '/' ) ); ?>">
                <div class="hero-search-icon">
                    <span class="material-symbols-outlined">search</span>
                </div>
                <input type="search" class="search-field" placeholder="Enter your destination" value="<?php echo get_search_query(); ?>" name="s">
                <button type="submit" class="btn btn-primary">Get a Quote</button>
            </form>
        </div>
    </div>

    <!-- Services Section -->
    <div class="w-full max-w-7xl mx-auto px-4 md:px-10 py-12 md:py-20" id="services">
        <h2 class="text-accent text-3xl font-bold leading-tight tracking-[-0.015em] px-4 pb-6 pt-5 text-center">Our Services</h2>
        <div class="grid grid-cols-1 sm:grid-cols-3 lg:grid-cols-3 gap-2">
            <?php
            $services_query = new WP_Query( array(
                'post_type' => 'service',
                'posts_per_page' => 3,
            ) );

            if ( $services_query->have_posts() ) :
                while ( $services_query->have_posts() ) : $services_query->the_post();
                    get_template_part( 'template-parts/content', 'service' );
                endwhile;
                wp_reset_postdata();
            else :
                echo '<p class="text-center col-span-3">No services found. Please add some in the admin.</p>';
            endif;
            ?>
        </div>
    </div>

    <!-- Cars Section -->
    <div class="w-full bg-background-light py-12 md:py-20" id="cars">
        <div class="w-full max-w-7xl mx-auto px-4 md:px-10">
            <h2 class="text-accent text-3xl font-bold leading-tight tracking-[-0.015em] px-4 pb-6 pt-5 text-center">Our Cars</h2>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-2">
                <?php
                $vehicles_query = new WP_Query( array(
                    'post_type' => 'vehicle',
                    'posts_per_page' => 3,
                ) );

                if ( $vehicles_query->have_posts() ) :
                    while ( $vehicles_query->have_posts() ) : $vehicles_query->the_post();
                        get_template_part( 'template-parts/content', 'vehicle' );
                    endwhile;
                    wp_reset_postdata();
                else :
                    echo '<p class="text-center col-span-3">No vehicles found.</p>';
                endif;
                ?>
            </div>
        </div>
    </div>

    <!-- Excursions Section -->
    <div class="w-full max-w-7xl mx-auto px-4 md:px-10 py-12 md:py-20" id="excursions">
        <h2 class="text-accent text-3xl font-bold leading-tight tracking-[-0.015em] px-4 pb-6 pt-5 text-center">Popular Excursions</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-2">
            <?php
            $excursions_query = new WP_Query( array(
                'post_type' => 'excursion',
                'posts_per_page' => 3,
            ) );

            if ( $excursions_query->have_posts() ) :
                while ( $excursions_query->have_posts() ) : $excursions_query->the_post();
                    get_template_part( 'template-parts/content', 'excursion' );
                endwhile;
                wp_reset_postdata();
            else :
                echo '<p class="text-center col-span-3">No excursions found.</p>';
            endif;
            ?>
        </div>
    </div>

    <!-- Reviews Section -->
    <div class="w-full bg-background-light py-12 md:py-20" id="reviews">
        <div class="w-full max-w-7xl mx-auto px-4 md:px-10">
            <h2 class="text-accent text-3xl font-bold leading-tight tracking-[-0.015em] px-4 pb-8 pt-5 text-center">What Our Travelers Say</h2>
            <!-- Placeholder for TripAdvisor Widget -->
            <div class="text-center p-10 border-2 border-dashed border-gray-300 rounded-xl">
                <p class="text-gray-500">WP TripAdvisor Review Slider will appear here.</p>
            </div>
        </div>
    </div>

    <!-- CTA Section -->
    <div class="w-full">
        <div class="w-full max-w-7xl mx-auto px-4 md:px-10 py-12 md:py-20 text-center">
            <div class="bg-accent text-white p-10 rounded-xl flex flex-col items-center gap-4">
                <h3 class="text-3xl font-bold">Have a Custom Itinerary in Mind?</h3>
                <p class="max-w-xl">We specialize in creating personalized tours and excursions tailored to your interests. Let us help you design your perfect Moroccan adventure.</p>
                <button class="btn btn-primary h-12 px-6 mt-4">
                    <span class="truncate">Contact Us for a Custom Tour</span>
                </button>
            </div>
        </div>
    </div>
</div>

<?php get_footer(); ?>
